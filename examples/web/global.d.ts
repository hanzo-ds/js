/* eslint-disable no-var */
// `declare var` is the standard way to declare ambient global variables.
declare var DATASTORE_CLOUD_URL: string;
declare var DATASTORE_CLOUD_PASSWORD: string;
declare var DATASTORE_CLUSTER_URL: string;
